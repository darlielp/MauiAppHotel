<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiAppHotel.Views.Sobre"
             Title="Sobre"
             NavigationPage.HasNavigationBar="False"
             BackgroundColor="{StaticResource Gray100}">

    <VerticalStackLayout VerticalOptions="Center" Padding="20" Spacing="15">
        <Label Text="Sobre o Aplicativo"
               FontSize="32"
               FontAttributes="Bold"
               TextColor="{StaticResource Primary}"
               HorizontalOptions="Center"/>

        <Label Text="Desenvolvido por: Darliel Pereira"
               FontSize="18"
               TextColor="Black"
               HorizontalOptions="Center"/>

        <Label Text="Ano de Desenvolvimento: 2025"
               FontSize="18"
               TextColor="Black"
               HorizontalOptions="Center"/>

        <Label Text="Aplicativo desenvolvido como parte do curso Técnico em Desenvolvimento de Sistemas."
               FontSize="16"
               TextColor="Gray"
               HorizontalTextAlignment="Center"/>

        <Button Text="Voltar"
                BackgroundColor="{StaticResource Primary}"
                TextColor="White"
                Clicked="Voltar_Clicked"/>
    </VerticalStackLayout>
</ContentPage>
